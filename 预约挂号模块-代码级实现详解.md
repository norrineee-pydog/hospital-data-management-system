# 预约挂号模块 — 代码级实现详解

> 用于毕业设计验收讲解，覆盖"患者预约挂号"完整链路：前端 → 后端 → 数据库触发器。

---

## 目录

1. [整体架构概览](#1-整体架构概览)
2. [数据库层（openGauss）](#2-数据库层opengauss)
3. [后端 API 层](#3-后端-api-层)
4. [前端页面层](#4-前端页面层)
5. [完整调用链路](#5-完整调用链路)
6. [核心重点：BEFORE INSERT 触发器详解](#6-核心重点before-insert-触发器详解)
7. [新增/修改文件清单](#7-新增修改文件清单)

---

## 1. 整体架构概览

```
┌─────────────────────────────────────────────────────────┐
│                      前端 (Vue3)                         │
│  src/views/hospital/register/index.vue                   │
│  四步流程: 选科室 → 选医生 → 选时段 → 确认挂号              │
│  src/api/hospital/appointment/index.ts   (5个API函数)      │
└──────────────────┬──────────────────────────────────────┘
                   │ HTTP REST
                   ▼
┌─────────────────────────────────────────────────────────┐
│                  后端 (Spring Boot)                      │
│  AppointmentController.java    (5个REST端点)              │
│  AppointmentServiceImpl.java   (业务逻辑+事务)             │
│  DoctorScheduleMapper.java     (3条自定义SQL)             │
└──────────────────┬──────────────────────────────────────┘
                   │ MyBatis-Plus
                   ▼
┌─────────────────────────────────────────────────────────┐
│               数据库 (openGauss)                         │
│  hospital_doctor_schedule 表                             │
│  hospital_visit 表 (+ schedule_id 列)                     │
│  ★ trg_visit_before_insert 触发器 (BEFORE INSERT)        │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 数据库层（openGauss）

### 2.1 新建表：`hospital_doctor_schedule`（医生排班表）

**文件位置：** `ruoyi-vue-pro/sql/opengauss/hospital_schedule_trigger.sql`

```sql
CREATE TABLE hospital_doctor_schedule (
    id             int8         NOT NULL,   -- 排班ID（主键）
    doctor_id      int8         NOT NULL,   -- 医生ID
    schedule_date  date         NOT NULL,   -- 排班日期（如 2026-06-15）
    period         varchar(50)  NOT NULL,   -- 时段（如 "08:00-09:00"）
    total_num      int4         NOT NULL DEFAULT 10,  -- 总号量
    available_num  int4         NOT NULL DEFAULT 10,  -- ★ 剩余号量（核心字段）
    creator        varchar(64)  NULL DEFAULT '',
    create_time    timestamp    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updater        varchar(64)  NULL DEFAULT '',
    update_time    timestamp    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    deleted        int2         NOT NULL DEFAULT 0,   -- 逻辑删除标记
    tenant_id      int8         NOT NULL DEFAULT 0
);
```

**关键字段说明：**

| 字段 | 说明 |
|------|------|
| `total_num` | 该时段的总号源数量（如 10） |
| `available_num` | ★ **剩余可挂号数量**，每次挂号成功减 1，由触发器自动维护 |
| `period` | 时段范围，如 `"08:00-09:00"`，用来区分上午/下午不同时段 |
| `schedule_date` | 排班的具体日期 |

### 2.2 修改表：`hospital_visit` 增加 `schedule_id`

```sql
ALTER TABLE hospital_visit
    ADD COLUMN IF NOT EXISTS schedule_id int8 NULL;
```

**作用：** 让每一条就诊记录关联到具体的排班时段，触发器通过这个字段找到对应的排班去扣减号源。

### 2.3 自增序列

```sql
CREATE SEQUENCE hospital_doctor_schedule_seq START 1;
```

用于排班表 `id` 字段的自增（`nextval('hospital_doctor_schedule_seq')`）。

---

## 3. 后端 API 层

### 3.1 REST 端点一览

**文件位置：** `AppointmentController.java`
**基础路径：** `/hospital/appointment`

| 方法 | 路径 | 参数 | 功能 |
|------|------|------|------|
| `GET` | `/get-department-list` | 无 | 获取所有科室列表 |
| `GET` | `/get-doctor-list` | `deptId` | 获取某科室下的医生列表 |
| `GET` | `/get-schedule-dates` | `doctorId` | 获取某医生的所有排班日期（去重） |
| `GET` | `/get-schedule-list` | `doctorId`, `date` | 获取某医生某日有剩余号源的排班 |
| `POST` | `/create` | JSON Body | **创建挂号（核心）** |

### 3.2 创建挂号 — 完整流程（`POST /create`）

**请求体 VO：** `AppointmentCreateReqVO.java`

```java
@Data
public class AppointmentCreateReqVO {
    @NotNull  private Long patientId;    // 患者ID（必填）
    @NotNull  private Long doctorId;     // 医生ID（必填）
    @NotNull  private Long deptId;       // 科室ID（必填）
    @NotNull  private Long scheduleId;   // ★ 排班ID（必填，触发器据此扣减号源）
              private String reason;     // 就诊原因（可选）
}
```

**Service 实现：** `AppointmentServiceImpl.java` → `createAppointment()` 方法

```java
@Override
@Transactional(rollbackFor = Exception.class)  // ← 事务保证原子性
public Long createAppointment(AppointmentCreateReqVO createReqVO) {

    // ===== 步骤 1：校验医生是否存在 =====
    DoctorDO doctor = doctorMapper.selectById(createReqVO.getDoctorId());
    if (doctor == null) {
        throw exception(DOCTOR_NOT_EXISTS);   // 错误码: 1_100_002_000
    }

    // ===== 步骤 2：校验排班是否存在且有余号 =====
    // 这是应用层的双重校验，触发器也会再检查一次
    DoctorScheduleDO schedule = doctorScheduleMapper.selectById(createReqVO.getScheduleId());
    if (schedule == null) {
        throw exception(..., "排班记录不存在");    // 错误码: 1004001002
    }
    if (schedule.getAvailableNum() <= 0) {
        throw exception(..., "该时段号源已满");    // 错误码: 1004001003
    }

    // ===== 步骤 3：构造 visit_date（排班日期 + 时段开始时间）=====
    // 例如：排班日期=2026-06-15, period="08:00-09:00"
    //       → visitDateTime = 2026-06-15T08:00:00
    String periodStart = schedule.getPeriod().split("-")[0];  // "08:00"
    LocalDate scheduleDate = schedule.getScheduleDate();       // 2026-06-15
    LocalTime startTime = LocalTime.parse(periodStart);        // 08:00:00
    LocalDateTime visitDateTime = LocalDateTime.of(scheduleDate, startTime);

    // ===== 步骤 4：创建就诊记录，状态为"待就诊" =====
    VisitDO visit = VisitDO.builder()
            .patientId(createReqVO.getPatientId())
            .doctorId(createReqVO.getDoctorId())
            .deptId(createReqVO.getDeptId())
            .scheduleId(createReqVO.getScheduleId())  // ★ 关联排班
            .visitDate(visitDateTime)
            .reason(createReqVO.getReason())
            .status("WAITING")                        // 初始状态：待就诊
            .build();
    visitMapper.insert(visit);  // ★ INSERT 触发数据库触发器，自动扣减号源

    return visit.getId();
}
```

**关键设计点：**

1. **`@Transactional` 事务注解**：保证 Java 层的 insert 和数据库触发器的 UPDATE（扣减号源）在同一个数据库事务中。如果触发器执行失败（如号源已满），INSERT 也会回滚。
2. **双重校验**：Java 代码先检查 `available_num > 0`，数据库触发器再检查一次（带行锁），防止并发超卖。
3. **错误码体系**：使用 `ErrorCodeConstants` 统一管理错误码，便于前端展示中文错误信息。

### 3.3 取消挂号（`cancelAppointment`）

**文件位置：** `AppointmentServiceImpl.java` → `cancelAppointment()` 方法

```java
@Override
public void cancelAppointment(Long visitId) {
    // 1. 校验就诊记录存在
    VisitDO visit = visitMapper.selectById(visitId);
    if (visit == null) {
        throw exception(VISIT_NOT_EXISTS);  // 错误码: 1_100_006_000
    }

    // 2. 只有"待就诊"状态可以取消
    if (!"WAITING".equals(visit.getStatus())) {
        throw exception(VISIT_STATUS_CANT_CANCEL);  // 错误码: 1004001001
    }

    // 3. 更新状态为"已取消"
    visit.setStatus("CANCELLED");
    visitMapper.updateById(visit);
}
```

### 3.4 Mapper 层 — 自定义 SQL 查询

**文件位置：** `DoctorScheduleMapper.java`

```java
// 查询1：获取某医生某日期的排班列表（所有排班，包括无号的）
@Select("SELECT * FROM hospital_doctor_schedule
         WHERE doctor_id = #{doctorId}
           AND schedule_date = #{scheduleDate}
           AND deleted = 0
         ORDER BY period")
List<DoctorScheduleDO> selectListByDoctorIdAndDate(...);

// 查询2：★ 获取某医生某日期有剩余号源的排班（available_num > 0）
@Select("SELECT * FROM hospital_doctor_schedule
         WHERE doctor_id = #{doctorId}
           AND schedule_date = #{scheduleDate}
           AND available_num > 0      -- ★ 只查有余号的
           AND deleted = 0
         ORDER BY period")
List<DoctorScheduleDO> selectAvailableByDoctorIdAndDate(...);

// 查询3：获取某医生的所有排班日期（去重，仅显示>=今天的日期）
@Select("SELECT DISTINCT schedule_date
         FROM hospital_doctor_schedule
         WHERE doctor_id = #{doctorId}
           AND deleted = 0
           AND schedule_date >= CURRENT_DATE   -- 不显示历史日期
         ORDER BY schedule_date")
List<String> selectDistinctDatesByDoctorId(...);
```

**数据对象 DO：**

- **`DoctorScheduleDO.java`**：映射 `hospital_doctor_schedule` 表，字段包括 `id`, `doctorId`, `scheduleDate`, `period`, `totalNum`, `availableNum`
- **`VisitDO.java`**：映射 `hospital_visit` 表，新增了 `scheduleId` 字段关联排班

---

## 4. 前端页面层

### 4.1 API 调用层

**文件位置：** `src/api/hospital/appointment/index.ts`

```typescript
import request from '@/config/axios'

// POST /hospital/appointment/create
export const createAppointment = (data: {
  patientId: number; doctorId: number; deptId: number;
  scheduleId: number; reason?: string;
}) => request.post({ url: '/hospital/appointment/create', data })

// GET /hospital/appointment/get-department-list
export const getDepartmentListForAppointment = () =>
  request.get({ url: '/hospital/appointment/get-department-list' })

// GET /hospital/appointment/get-doctor-list?deptId=xxx
export const getDoctorListForAppointment = (deptId: number) =>
  request.get({ url: '/hospital/appointment/get-doctor-list?deptId=' + deptId })

// GET /hospital/appointment/get-schedule-list?doctorId=xxx&date=yyy
export const getScheduleListForAppointment = (doctorId: number, date: string) =>
  request.get({ url: '/hospital/appointment/get-schedule-list?doctorId=' + doctorId + '&date=' + date })

// GET /hospital/appointment/get-schedule-dates?doctorId=xxx
export const getScheduleDatesForAppointment = (doctorId: number) =>
  request.get({ url: '/hospital/appointment/get-schedule-dates?doctorId=' + doctorId })
```

### 4.2 挂号页面 — 四步流程

**文件位置：** `src/views/hospital/register/index.vue`

#### 页面状态变量

```typescript
const activeStep = ref(0)          // 当前步骤 (0~3)
const selectedDept = ref(null)     // 选中的科室
const selectedDoctor = ref(null)   // 选中的医生
const selectedDate = ref('')       // 选中的排班日期
const selectedSchedule = ref(null) // 选中的排班时段
const confirmForm = reactive({     // 第四步的表单
  patientId: undefined,            // 患者ID（用户输入）
  reason: ''                       // 就诊原因（可选）
})
```

#### Step 0：选择科室

- 页面加载时自动调用 `getDepartmentListForAppointment()` → 展示所有科室卡片
- 卡片显示：科室名称、主任、位置
- 点击卡片 → `selectDepartment(dept)` → 记录选中科室 → 自动进入 Step 1
- 选中态：蓝色边框 + 浅蓝背景（CSS class `.dept-card.selected`）

```typescript
const selectDepartment = (dept) => {
  selectedDept.value = dept
  selectedDoctor.value = null     // 清空后续选择
  selectedSchedule.value = null
  selectedDate.value = ''
  scheduleList.value = []
  nextStep()                      // activeStep → 1
}
```

#### Step 1：选择医生

- 进入时自动调用 `getDoctorListForAppointment(selectedDept.value.id)` → 展示该科室的医生
- 卡片显示：头像、姓名、职称、性别/年龄
- 点击卡片 → `selectDoctor(doctor)` → 记录选中医生 → 自动加载排班日期 → 进入 Step 2

```typescript
const selectDoctor = (doctor) => {
  selectedDoctor.value = doctor
  selectedSchedule.value = null
  selectedDate.value = ''
  scheduleList.value = []
  scheduleDates.value = []
  loadScheduleDates()            // 自动加载该医生的排班日期
  nextStep()                     // activeStep → 2
}
```

#### Step 2：选择日期和时段

这是最复杂的一步，分为两个子步骤：

**（1）选择日期：**
- 调用 `getScheduleDatesForAppointment(doctorId)` → 获取该医生的所有可用日期
- 用 `el-radio-group` 展示为日期按钮，默认选中第一个

```typescript
const loadScheduleDates = async () => {
  const res = await getScheduleDatesForAppointment(selectedDoctor.value.id)
  scheduleDates.value = res || []
  if (scheduleDates.value.length > 0) {
    selectedDate.value = scheduleDates.value[0]  // 默认选中第一个日期
    loadSchedules()                               // 加载该日期的时段
  }
}
```

**（2）选择时段：**
- 调用 `getScheduleListForAppointment(doctorId, date)` → 获取该医生该日有剩余号源的时段
- 时段卡片显示：时段（如 "08:00-09:00"）+ 剩余号量/总号量
- 剩余 ≤ 3 时红色高亮 `.num-warning { color: #f56c6c }` 提示号源紧张

```typescript
const loadSchedules = async () => {
  const res = await getScheduleListForAppointment(
    selectedDoctor.value.id,
    selectedDate.value
  )
  scheduleList.value = res || []
}
```

#### Step 3：确认挂号

- 展示汇总信息：科室名、医生名、职称、就诊日期、就诊时段（均为 `disabled` 只读）
- 用户输入：患者ID（`v-model.number="confirmForm.patientId"`，必填）、就诊原因（`textarea`，可选）
- 点击"确认挂号" → `submitAppointment()`

```typescript
const submitAppointment = async () => {
  // 1. 前端校验
  if (!confirmForm.patientId) {
    ElMessage.warning('请输入患者ID')
    return
  }

  // 2. 调用后端创建挂号
  await createAppointment({
    patientId: confirmForm.patientId,
    doctorId: selectedDoctor.value.id,
    deptId: selectedDept.value.id,
    scheduleId: selectedSchedule.value.id,   // ★ 核心：关联排班ID
    reason: confirmForm.reason
  })

  // 3. 成功后重置
  ElMessage.success('挂号成功！')
  resetAll()  // 回到 Step 0，清空所有选择
}
```

#### 交互细节

| 场景 | 处理方式 |
|------|---------|
| 加载中 | 每个步骤独立 `v-loading`，不会整页阻塞 |
| 空数据 | 各步骤有空态提示："暂无可用科室"/"该科室暂无医生"/"该日期无可用号源" |
| API 失败 | `catch` 后显示 `ElMessage.error`，错误信息来自后端响应 |
| 选中高亮 | CSS 统一：蓝色边框 `#409eff` + 浅蓝背景 `#ecf5ff` |
| 上下一步 | 底部"上一步"/"下一步"按钮 + 步骤标题中的"< 返回"链接 |
| 号源紧张 | 剩余 ≤ 3 时数字变红，提醒用户尽快挂号 |

---

## 5. 完整调用链路

以一次完整的挂号操作为例：

```
用户在页面操作：
  ① 点击科室卡片（"内科"）     → GET /hospital/appointment/get-doctor-list?deptId=1
  ② 点击医生卡片（"李明华"）    → GET /hospital/appointment/get-schedule-dates?doctorId=1
                                   GET /hospital/appointment/get-schedule-list?doctorId=1&date=2026-06-15
  ③ 点击时段卡片（"08:00-09:00"）
  ④ 输入患者ID，点击"确认挂号" → POST /hospital/appointment/create
       Body: { patientId:1, doctorId:1, deptId:1, scheduleId:1, reason:"" }

后端处理（AppointmentServiceImpl.createAppointment）：
  ① 校验医生存在: doctorMapper.selectById(1) → 存在 ✓
  ② 校验排班有余号: doctorScheduleMapper.selectById(1) → availableNum=10 > 0 ✓
  ③ 构造 visitDateTime = 2026-06-15T08:00
  ④ 插入 hospital_visit 记录:
       INSERT INTO hospital_visit (patient_id, doctor_id, dept_id, schedule_id, visit_date, status)
       VALUES (1, 1, 1, 1, '2026-06-15 08:00:00', 'WAITING');

数据库触发器自动执行（trg_visit_before_insert）：
  ① SELECT available_num FROM hospital_doctor_schedule WHERE id=1 FOR UPDATE;  -- 行锁
  ② 检查: available_num=10 > 0 ✓
  ③ UPDATE hospital_doctor_schedule SET available_num=9 WHERE id=1;
  ④ RETURN NEW;  -- 允许 INSERT 继续

返回结果：
  200 OK → 前端显示 "挂号成功！" → 回到 Step 0
```

---

## 6. 核心重点：BEFORE INSERT 触发器详解

> 这是整个模块中**最核心的数据库机制**，保证并发场景下号源不会被超卖。

### 6.1 触发器函数（完整代码+逐行注释）

```sql
CREATE OR REPLACE FUNCTION trg_func_visit_before_insert()
RETURNS TRIGGER AS
$$
DECLARE
    v_available int4;   -- 声明变量：存储当前剩余号量
BEGIN
    -- ★ 只有当 schedule_id 不为空时才触发扣减（兼容旧数据）
    IF NEW.schedule_id IS NOT NULL THEN

        -- ★ 步骤1：查询排班剩余号量 + 加行级锁（FOR UPDATE）
        -- SELECT ... FOR UPDATE 是并发控制的关键：
        --   事务A未提交前，事务B执行到这里会被阻塞，等待事务A释放锁
        --   这就保证了 不会有两个事务同时读到相同的 available_num
        SELECT available_num INTO v_available
        FROM hospital_doctor_schedule
        WHERE id = NEW.schedule_id AND deleted = 0
        FOR UPDATE;

        -- ★ 步骤2：排班不存在 → 抛出异常，回滚 INSERT
        IF NOT FOUND THEN
            RAISE EXCEPTION '排班记录不存在 (schedule_id: %)', NEW.schedule_id;
        END IF;

        -- ★ 步骤3：剩余号量 ≤ 0 → 抛出异常，回滚 INSERT
        IF v_available <= 0 THEN
            RAISE EXCEPTION '该时段号源已满，无法挂号';
        END IF;

        -- ★ 步骤4：扣减号量（available_num - 1）
        UPDATE hospital_doctor_schedule
        SET available_num = available_num - 1,
            update_time = CURRENT_TIMESTAMP
        WHERE id = NEW.schedule_id AND deleted = 0;
    END IF;

    -- ★ 返回 NEW，允许 INSERT 操作继续执行
    RETURN NEW;
END;
$$
LANGUAGE plpgsql;
```

### 6.2 触发器的挂载

```sql
CREATE TRIGGER trg_visit_before_insert
    BEFORE INSERT ON hospital_visit   -- ★ 在 hospital_visit 表每次 INSERT 之前执行
    FOR EACH ROW                      -- ★ 每插入一行就执行一次
    EXECUTE FUNCTION trg_func_visit_before_insert();
```

**三个关键点的解释：**

| 关键字 | 含义 |
|--------|------|
| `BEFORE INSERT` | 在数据真正写入磁盘之前执行触发器，如果触发器报错，INSERT 不会执行 |
| `FOR EACH ROW` | 行级触发器，每插入一行就触发一次（不是语句级） |
| `FOR UPDATE` | 行级排他锁，阻止其他事务同时修改/读取该行，保证并发安全 |

### 6.3 并发场景分析：为什么不会超卖

假设 `available_num = 1`（只剩最后一个号），两个用户同时挂号：

```
时间轴 →

事务A（用户1）:                        事务B（用户2）:
  BEGIN                                  BEGIN
  INSERT INTO hospital_visit ...         INSERT INTO hospital_visit ...
    ↓ 触发器触发                            ↓ 触发器触发
    SELECT ... FOR UPDATE                   SELECT ... FOR UPDATE
    ↓ 获得行锁 ✓                            ↓ ★ 被阻塞！等待事务A释放锁
    读到 available_num = 1                   （挂起等待...）
    检查: 1 > 0 ✓
    UPDATE SET available_num = 0
  COMMIT（释放行锁）                           ↓ 获得行锁 ✓
                                              读到 available_num = 0
                                              检查: 0 > 0 ✗
                                              RAISE EXCEPTION '号源已满'
                                            ROLLBACK
  结果: 挂号成功                               结果: 挂号失败，提示号源已满
```

**核心机制：** `FOR UPDATE` 行锁 + 事务隔离级别 = 不会出现两个事务同时读到 `available_num = 1` 的情况。

### 6.4 为什么还要在 Java 代码里校验一次？

这是**双重保险**的设计：

1. **Java 层校验（快速失败）**：如果号源已满，在应用层就抛出异常，不需要走到数据库，减少数据库压力。
2. **数据库触发器校验（最终保障）**：即使 Java 层校验和 INSERT 之间有时间差（极端并发），触发器的 `FOR UPDATE` 行锁也能兜底。

两层校验一起保证了数据一致性。

---

## 7. 新增/修改文件清单

### 7.1 数据库 SQL

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `ruoyi-vue-pro/sql/opengauss/hospital_schedule_trigger.sql` | **新增** | ★ 排班表建表 + 触发器 + 测试数据（核心文件） |

### 7.2 后端 Java

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `.../controller/admin/appointment/AppointmentController.java` | **新增** | REST 控制器，5个接口端点 |
| `.../controller/admin/appointment/vo/AppointmentCreateReqVO.java` | **新增** | 创建挂号请求 VO（patientId, doctorId, deptId, scheduleId, reason） |
| `.../controller/admin/appointment/vo/DoctorScheduleRespVO.java` | **新增** | 排班响应 VO（id, doctorId, scheduleDate, period, totalNum, availableNum） |
| `.../service/appointment/AppointmentService.java` | **重写** | 服务接口（6个方法定义） |
| `.../service/appointment/AppointmentServiceImpl.java` | **新增** | 服务实现（创建挂号、取消挂号、查询科室/医生/排班） |
| `.../dal/dataobject/DoctorScheduleDO.java` | **新增** | 排班表数据对象 |
| `.../dal/dataobject/VisitDO.java` | **修改** | 新增 `scheduleId` 字段 |
| `.../dal/mysql/DoctorScheduleMapper.java` | **新增** | 排班表 Mapper（3条自定义 @Select SQL） |
| `.../enums/ErrorCodeConstants.java` | **修改** | 新增 `VISIT_STATUS_CANT_CANCEL` 错误码 |

### 7.3 前端 Vue3/TypeScript

| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `yudao-ui-admin-vue3/src/api/hospital/appointment/index.ts` | **新增** | 挂号 API 封装（5个函数） |
| `yudao-ui-admin-vue3/src/views/hospital/register/index.vue` | **新增** | ★ 挂号页面（四步流程，约520行） |
| `yudao-ui-admin-vue3/src/views/hospital/my-registration/index.vue` | **新增** | "我的挂号记录"页面 |

### 7.4 目录结构总览

```
project/
├── ruoyi-vue-pro/sql/opengauss/
│   └── hospital_schedule_trigger.sql          ★ 数据库核心
│
├── ruoyi-vue-pro/yudao-module-hospital/src/main/java/.../
│   ├── controller/admin/appointment/
│   │   ├── AppointmentController.java          REST控制器
│   │   └── vo/
│   │       ├── AppointmentCreateReqVO.java     请求VO
│   │       └── DoctorScheduleRespVO.java       排班响应VO
│   ├── service/appointment/
│   │   ├── AppointmentService.java             服务接口
│   │   └── AppointmentServiceImpl.java         ★ 业务逻辑核心
│   ├── dal/dataobject/
│   │   ├── DoctorScheduleDO.java              排班数据对象
│   │   └── VisitDO.java                       [修改] +scheduleId
│   ├── dal/mysql/
│   │   └── DoctorScheduleMapper.java          排班SQL查询
│   └── enums/
│       └── ErrorCodeConstants.java            [修改] +挂号错误码
│
└── yudao-ui-admin-vue3/src/
    ├── api/hospital/appointment/
    │   └── index.ts                           前端API封装
    └── views/hospital/
        └── register/
            └── index.vue                      ★ 前端四步挂号页面
```

---

## 附录：验收演示要点

向老师讲解时，建议按以下顺序：

1. **先说数据库**：拿起触发器 SQL，讲 `FOR UPDATE` 行锁 + `BEFORE INSERT` 的设计思路（这是最大亮点）。
2. **再说后端**：`createAppointment` 的四步校验 + `@Transactional` 事务注解。
3. **然后说前端**：打开页面，展示四步流程的实际运行效果。
4. **最后总结**：三重数据一致性保障（前端校验 → Java 双重校验 → 数据库触发器行锁），说明为什么系统不会出现超卖。