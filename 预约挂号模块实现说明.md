# 预约挂号模块 - 实现说明

## 概述

实现"预约挂号核心流水线"功能，支持患者通过四步流程完成在线挂号：**选择科室 → 选择医生 → 选择排班时段 → 确认挂号**。同时通过 openGauss 数据库触发器自动维护号源扣减，保证数据一致性。

---

## 一、数据库层（openGauss）

### 1.1 新建表：`hospital_doctor_schedule`（医生排班表）

| 列名 | 类型 | 说明 |
|------|------|------|
| `id` | int8 PK | 排班ID，自增序列 |
| `doctor_id` | int8 NOT NULL | 医生ID |
| `schedule_date` | date NOT NULL | 排班日期 |
| `period` | varchar(50) NOT NULL | 时段，如 `08:00-09:00` |
| `total_num` | int4 NOT NULL DEFAULT 10 | 总号量 |
| `available_num` | int4 NOT NULL DEFAULT 10 | 剩余号量 |
| `deleted` | int2 DEFAULT 0 | 逻辑删除 |
| `tenant_id` | int8 DEFAULT 0 | 租户ID |
| + 标准 BaseDO 字段 | | |

### 1.2 修改表：`hospital_visit`

```sql
ALTER TABLE hospital_visit ADD COLUMN schedule_id int8 NULL;
```

### 1.3 BEFORE INSERT 触发器

当 `hospital_visit` 表插入新记录时，触发器自动执行：

1. 根据 `NEW.schedule_id` 查找排班记录（带行锁 `FOR UPDATE`）
2. 检查排班是否存在 → 不存在则报错
3. 检查 `available_num > 0` → 已满则报错
4. 执行 `available_num = available_num - 1`

### 1.4 测试数据

为 2 位医生生成了约 14 条排班记录，覆盖近 3 天多个时段。

---

## 二、后端 API

### 2.1 请求端点

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/hospital/appointment/get-department-list` | 获取全科室列表 |
| `GET` | `/hospital/appointment/get-doctor-list?deptId=X` | 获取某科室下医生列表 |
| `GET` | `/hospital/appointment/get-schedule-dates?doctorId=X` | 获取某医生的排班日期（去重） |
| `GET` | `/hospital/appointment/get-schedule-list?doctorId=X&date=Y` | 获取某医生某日可用排班（`available_num > 0`） |
| `POST` | `/hospital/appointment/create` | **创建挂号** |

### 2.2 创建挂号流程（`POST /hospital/appointment/create`）

```
请求体: { patientId, doctorId, deptId, scheduleId, reason }
```

1. **校验** 医生存在
2. **校验** 排班存在且 `available_num > 0`
3. **构造** `visit_date` = 排班日期 + 时段开始时间（如 `2026-06-13T08:00`）
4. **插入** `hospital_visit` 记录，状态为 `WAITING`
5. **触发器** 自动扣减 `available_num`
6. 返回 `visitId`

事务注解 `@Transactional(rollbackFor = Exception.class)` 保证原子性。

### 2.3 新增/修改文件

```
ruoyi-vue-pro/yudao-module-hospital/
├── controller/admin/
│   ├── appointment/
│   │   ├── AppointmentController.java          [新增]
│   │   └── vo/
│   │       ├── AppointmentCreateReqVO.java     [新增]
│   │       └── DoctorScheduleRespVO.java       [新增]
│   ├── department/vo/
│   │   └── DepartmentRespVO.java               [新增]
│   └── visit/vo/
│       └── VisitRespVO.java                    [修改 - 加 scheduleId]
├── dal/
│   ├── dataobject/
│   │   ├── DoctorScheduleDO.java               [新增]
│   │   └── VisitDO.java                        [修改 - 加 scheduleId]
│   └── mysql/
│       └── DoctorScheduleMapper.java           [新增]
└── service/appointment/
    ├── AppointmentService.java                  [重写]
    └── AppointmentServiceImpl.java             [新增]
```

---

## 三、前端页面

### 3.1 四步挂号流程

```
Step 1: 选择科室
   ├── 卡片网格展示所有科室
   ├── 点击选中 → 自动进入下一步
   └── 支持返回重选

Step 2: 选择医生
   ├── 卡片展示医生头像/姓名/职称/性别/年龄
   ├── 展示当前科室名
   └── 点击选中 → 自动加载排班日期

Step 3: 选择时段
   ├── 日期按钮组（RadioButton）切换
   ├── 时段卡片展示 "08:00-09:00" + 剩余/总号量
   ├── 剩余号量 ≤ 3 时红色高亮
   ├── 无号源时显示空提示
   └── 点击选中时段

Step 4: 确认挂号
   ├── 汇总展示：科室、医生、职称、日期、时段
   ├── 输入患者ID（必填）
   ├── 输入就诊原因（可选）
   └── 确认按钮 → 提交 → 成功后重置到第一步
```

### 3.2 状态管理

- 加载中状态（`v-loading`）：每个步骤独立控制
- 选中态高亮（蓝色边框 + 浅蓝背景）
- 空数据提示："暂无可用科室" / "该科室暂无医生" / "该日期无可用号源"
- 错误处理：API 失败时显示 `ElMessage.error`
- 提交成功：`ElMessage.success` + 自动重置表单

### 3.3 修改文件

```
yudao-ui-admin-vue3/src/
├── api/hospital/appointment/index.ts     [重写 - 新增5个API]
└── views/hospital/register/index.vue    [重写 - 四步流程]
```

---

## 四、数据一致性保证

| 层级 | 机制 |
|------|------|
| **数据库触发器** | `BEFORE INSERT` + `FOR UPDATE` 行锁，保证并发下号源不超卖 |
| **应用层校验** | 插入前检查 `available_num > 0`，双重校验 |
| **事务** | `@Transactional` 保证 insert + trigger 扣减在同一个事务中 |
| **逻辑删除** | 所有查询均带 `deleted = 0` 条件 |

---

## 五、部署执行顺序

```bash
# 1. 执行 SQL（建表 + 触发器 + 测试数据）
docker exec opengauss bash -c "export LD_LIBRARY_PATH=/usr/local/opengauss/lib; \
  /usr/local/opengauss/bin/gsql -U gaussdb -W Hospital@2026 -d ruoyi_vue_pro \
  -f /tmp/hospital_schedule_trigger.sql"

# 2. 重启后端（自动重新部署）
# 刷新页面后即可访问挂号功能
```